#  作业：AI狼人杀一多智能体协作与博弈的AgentTeam实战
- 基于多Agent协作框架，构建一个能够自主完成信息不对称博弈的狼人杀AgentTeam系统。课题核心在于多智能体的协作/对抗与交互机制设计：
    - 每个Agent根据其扮演角色（狼人、预言家、女巫等）拥有独立的目标、策略与行动空间，在严格信息隔离的约束下进行推理、发言与决策。
    - 系统需搭建完整的对局引擎，驱动回合流转与胜负裁决，并输出结构化日志以实现全程可观测。
    - 加分项为前端观战UI，支持纯Al对战或人机混战，直观呈现多Agent的实时博弈过程。
  - 进阶方向（三选一）：
    - 通用Agent一探索"读懂自己一修改自己一运行自己"的自演化系统，从「通用Agent」演化为「狼人杀多角色Agent」；
    - 评测+复盘一构 建结果评测、过程评测等多维可量化评测体系，基于此完成任意游戏的复盘归因，并产出不同版本、不同模型Agent同台竞技的Leaderboard；
    - 自进化Agent一实现”对局一分析一优化再对局"自进化循环，使各角色Agent在多局迭代中持续提升胜率。

# 开发提示词

## 第一轮提示词
我现在要在当前文件夹下开发一个AI狼人杀游戏，这是一个多智能体协作与博弈的AgentTeam，业务要求如下：
- 基于多Agent协作框架，构建一个能够自主完成信息不对称博弈的狼人杀AgentTeam系统。其核心在于多智能体的协作/对抗与交互机制设计
- 每个Agent根据其扮演角色（狼人、预言家、女巫等）拥有独立的目标、策略与行动空间，在严格信息隔离的约束下进行推理、发言与决策。
- 系统需搭建完整的对局引擎，驱动回合流转与胜负裁决，并输出结构化日志以实现全程可观测。
- 通过前端观战UI，支持纯Al对战或人机混战，直观呈现多Agent的实时博弈过程。
- 每个Agent都有"读懂自己一修改自己一运行自己"的自演化系统，从「通用Agent」演化为「狼人杀多角色Agent」

本地开发环境：
- 使用windows11系统；
- python环境使用conda管理，并使用名称为：py312的环境进行开发；
- 前端使用vue3，本地已经安装node v22.22.3和npm 10.9.8；
- 本地存在huggingface缓存，路径为：C:\Users\peng\.cache\huggingface，开发时优先使用本地缓存模型
- 本地docker desktop已经启动，必要时可以使用docker进行所需环境配置，避免污染本地windows系统

开发步骤要求：
- 项目从0-1进行开发
- 使用敏捷开发方式，项目拆分多个阶段，每个阶段都产出一个可执行版本，不断迭代

输出：
- 给出要实现这个项目最合适的技术栈和项目架构图，
- 结合业务、技术架构、开发步骤要求，生成开发计划
- 检查当前环境是否满足开发，若还需要本地部署其他环境或者模型，请给出手动安装步骤

要求：
只回答问题，不新增/删除/修改任何文件


## 第二轮提示词
环境配置要求如下：
- 使用conda中的py312环境，下载依赖等操作由你自动控制
- 不要使用本地模型进行推理，要使用线上deepseek-v4-flash进行推理
- 前端和后端项目初始化都由你自动完成

要求：
- 系统功能不要太复杂，可以对当前系统架构进行优化，最终生成画面简洁、代码结构简单的最小mvp即可
- 你对文件的增删改权限，仅限于当前项目目录之下，禁止操作项目以外目录文件
- 项目路径下新建docs文件夹，用于存储指导开发的文件，包括但不限于开发目标，开发要求（架构、开发环境等）、开发进度、开发计划（开发提示词、测试目标等）等信息
- 敏捷开发每个阶段都要包含根据开发计划进行自动开发和自动测试，保证每个阶段的版本可正常运行

将以上项目信息和规划等，结合本次要求进行调整，并在docs文件夹下生文档。不要开发代码，每次版本迭代由我手动控制。
